[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "apertus_data"
version = "0.1.0"
requires-python = ">=3.11"

[project.scripts]
apertus_data = "apertus_data.cli:main"

[tool.setuptools.packages.find]
include = ["apertus_data*"]
